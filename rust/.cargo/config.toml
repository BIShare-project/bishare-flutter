# Enable ARMv8 AES + PMULL hardware intrinsics in RustCrypto on aarch64 (Apple
# silicon, modern Android/ARM). Without these, AES-256-GCM runs ~11× slower.
[target.'cfg(target_arch = "aarch64")']
rustflags = ["--cfg", "aes_armv8", "--cfg", "polyval_armv8"]
